# import os
# import shutil
# import glob


def move_dirs(dirname: str = ""):
    # 指定したフォルダの有無を確認して存在しない場合は作成して処理を進める
    pass

    # 完全なファイルコピー
    # shutil.copy2(コピー元, コピー先)

    # ファイルの移動
    # shutil.move(移動元, 移動先)


if __name__ == "__main__":
    move_dirs()
